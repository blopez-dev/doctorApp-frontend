import styled from 'styled-components';

const LogoWraper = styled.div`
  max-width:10%;
  margin: 8px 15px;
  img{
    display:block;
    position:relative;
    max-width:150px;
    max-height: 40px;
  }
`;
export default LogoWraper;
